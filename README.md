# Fishbook Learning
This is a repo about *deep learning*, the reference book is "Fishbook".

## Installation
Install `Numpy` and `matplot` by the following code in bash.
```bash
pip install numpy
pip install matplotlib
```
